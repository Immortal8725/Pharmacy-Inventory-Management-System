# HealthFirst PIMS database configuration
# Use "h2" for the bundled embedded database (no MySQL install required).
# Use "mysql" after running database.sql against a local MySQL server.

db.mode=h2

mysql.host=localhost
mysql.port=3306
mysql.database=healthfirst_pims
mysql.user=root
mysql.password=
